{% extends '_objects/gridObject.twig' %}
{% block grid_items %}
<div class="grid_4col_tpl_01 grid-x medium-up-2 large-up-4 align-center {{ alignment|default }} {{ no_padding == 0 ? ' grid-padding-x grid-padding-y' }}">
    {% if items|default %}
        {% for key, item in items %}
            <div class="cell">
                {{ item }}
            </div>
        {% endfor %}
    {% endif %}
</div>
{% endblock %}
