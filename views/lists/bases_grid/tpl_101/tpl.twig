{% extends '_objects/visualModifier.twig' %}
{% block content %}
{% if is_list %}
    <li class="grid-container">
    {% endif %}
    {% if menu_part_title|default %}
        <span class="menuPart-title">{{ menu_part_title }}</span>
    {% endif %}
        {% if empty|default %}
        <div class="bg-lightgray padd-all-sm flex-container">
            <span class="h5 align-center align-middle">{{ empty }}</span>
        </div>
    {% endif %}
    <div class="{% block grid_class %}woody_bases_grid tpl_101{% endblock %} align-center grid-x{{ no_padding == 0 ? ' grid-padding-x grid-padding-y' }}">
        {% if items|default %}
            {% for key, item in items %}
                <div class="cell">
                    {% include "cards/#{ card }/tpl.twig" with {
                        'image_style' : image_style,
                        'page_type' : item.page_type
                    } %}
                </div>
            {% endfor %}
        {% endif %}
    </div>
    {% if is_list %}
    </li>
{% endif %}
{% endblock %}
